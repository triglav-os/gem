Portable GEM desktop implementation.

This directory now contains the replacement desktop app that uses only
the hosted `aes`, `vdi`, and `platform/os` wrappers.

The archived historical desktop port lives in [archive/desktop](/home/tstih/data/triglav-os/gem/archive/desktop).
